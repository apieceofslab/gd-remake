export function draw(ctx, player, state) {
  ctx.clearRect(0,0,innerWidth,innerHeight);

  ctx.fillStyle = "#111";
  ctx.fillRect(0,500,innerWidth,500);

  ctx.fillStyle = "white";
  ctx.fillRect(player.x - state.cameraX, player.y, player.size, player.size);
}
